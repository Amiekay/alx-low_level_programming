#include <stdio.h>
/**
 * main - prints program's name
 * Return: (0) for success
 * @argc: argument count
 * @argv: argument vector
*/
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
